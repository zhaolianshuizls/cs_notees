extern int v;  // global variables should declared with extern
// int v; // it is not declaration, but definition; so if it's included
          // more than once, will raise more than one definition
void reset();
